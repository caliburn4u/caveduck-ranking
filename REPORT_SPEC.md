# Caveduck 활성도 일일 보고서 — 산출 명세 (외부 감사용)

버전: 2026-08-30  
대상 산출물: `https://caliburn4u.github.io/caveduck-ranking/reports/YYYY-MM-DD.html`  
구현: `scripts/report_core.py`, `scripts/report_template.py`  
이력: `data/history.json`  
당일 원본: `data/latest.json`

이 문서는 보고서에 **어떤 섹션이 무엇을 어떤 식으로 계산해 보여주는지**를 코드와 1:1로 고정한다.  
감사 시 화면 문구와 이 명세가 다르면 화면이 아니라 **이 명세 + 소스코드**를 기준으로 삼는다.

---

## 0. 보고서가 측정하는 것 / 측정하지 않는 것

### 측정 대상
케이브덕 1일 인기 랭킹 **상위 20개 캐릭터의 채팅 수 합산값**(이하 Top20 합산)을, 플랫폼 전체 활성도의 **참고 지표**로 추적한다.

질문 네 가지에만 답한다.

1. 오늘 활성도는 어느 정도인가  
2. 어제보다 얼마나 변했는가  
3. 평소(7일·주간·30일·요일)보다 높은가 낮은가  
4. 지금은 단기/주간 어느 방향으로 움직이는가  

데이터가 30일 이상이면 다섯 번째:

5. 활성도 지수(Beta)로 환산하면 몇 점인가  

### 측정하지 않는 것
개별 캐릭터 이름·순위·챗수, 급상승 캐릭터, 신규 진입, 점유율, Top3/Top5, 장르 분석은 **의도적으로 포함하지 않는다.**

### 한계 (반드시 고지)
- Top20 합산 ≠ 플랫폼 전체 채팅 수. 하위권·비공개·미노출 캐릭터는 빠진다.  
- 로그인 후 NSFW 포함 랭킹을 Skyvern이 긁은 값이다. 비로그인 Safety 목록과는 다르다.  
- 수집 시각은 매일 약 22:00 KST 전후 한 시점의 스냅샷이다. 일중 변동은 없다.  
- 결측일이 있으면 연속 통계(연속 상승, 주간 평균)가 왜곡될 수 있다.

---

## 1. 데이터 파이프라인

```
22:00 KST  Skyvern가 caveduck.io 인기 탭(로그인) 스크레이프
       → Make.com 웹훅
       → GitHub PUT data/latest.json
22:05 KST  보고서 스킬
       → latest.json 날짜가 오늘인지 확인 (실패 시 최대 3회/90초 간격 재시도)
       → total_top20 검증(normalize_latest)
       → data/history.json에 일자 upsert
       → HTML 생성 → reports/YYYY-MM-DD.html 업로드
       → 약 1분 후 Discord 축약 임베드
```

### 1.1 원본 필드 (`latest.json`)
| 필드 | 의미 |
|------|------|
| `date` | 스냅샷 날짜 `YYYY-MM-DD` |
| `total_top20` | 파이프라인이 보낸 합산값 |
| `top20[]` | `{rank, name, chats}` 목록. 보고서 본문에는 쓰지 않고 **검증용으로만** 사용 |

### 1.2 검증 `normalize_latest`
1. `top20`이 문자열이어도 JSON/리터럴 파싱을 시도한다.  
2. `sum(top20.chats)`를 구한다.  
3. `top20`이 15개 이상이고 `total_top20 ≠ 합산`이면 **합산값을 채택**하고 `corrected=true`.  
4. 합산을 구할 수 없으면 저장된 `total_top20`만 사용.  
5. `total <= 0` 또는 `total > 200000`이면 이상치 플래그만 남긴다(값은 유지).  

history 병합 시 **더 큰 total을 우선하지 않는다.** 과거 오류 총합이 남는 것을 막기 위함이다.

### 1.3 시각 `time_kst`
보고서의 시각은 **보고서 작성 시각이 아니라** GitHub `data/latest.json` 최신 커밋 시각(UTC → KST `HH:MM`)이다.

### 1.4 분석 집합 `eligible`
`history.json`에서 `date <= 보고서일자`인 행만 날짜 오름차순으로 사용한다.  
관측일수 `n = len(eligible)`.

---

## 2. 공통 기호

`T` = 오늘 Top20 합산  
`Y` = 전일 Top20 합산  
`n` = 관측일수  

변화량: `Δ = T − Y`  
변화율: `Δ% = Δ / Y × 100`  (전일 0이면 0)

퍼센트는 소수 첫째 자리. 인원/합산은 반올림 정수 + 천 단위 콤마.  
주간 차이처럼 `.0`이 의미 없는 값은 정수로 표시한다.

날짜 표기: `MM-DD (요일)` 예: `08-30 (일)`

---

## 3. 섹션별 명세

보고서는 위에서 아래로 다음 순서다.

1. HEADER  
2. TODAY  
3. CURRENT POSITION  
4. QUICK STATUS  
5. TODAY'S BRIEFING  
6. ACTIVITY INDEX (n≥30일만)  
7. ACTIVITY TREND (7일 / 당월)  
8. DAILY CHANGE  
9. COMPARISONS  
10. WEEKDAY PATTERN · 최근 4주 (조건 충족 시)  
11. RECENT RECORDS  
12. DATA STATUS / METHODOLOGY (접힘)  
13. FOOTER  

---

### 3.1 HEADER

| 표시 | 출처 |
|------|------|
| 제목 | 고정: 케이브덕 활성도 일일 보고 |
| 날짜·요일·시각 | `date`, 요일, `time_kst` |
| 태그 | `Top20 채팅 합산 · 플랫폼 활성도 참고 지표 · 관측 n일` |
| `@4leaf` | `https://caveduck.io/user/4Leaf` |

계산 없음.

---

### 3.2 TODAY

표시 항목만 둔다. 변화 강도 문구는 여기 넣지 않는다.

| 항목 | 식 |
|------|----|
| 상위20 총합 | `T` |
| 전일 대비 | `Δ` |
| 변화율 | `Δ%` |
| 7일 평균 대비 | `(T − avg7) / avg7 × 100` |

`avg7` = 최근 최대 7일의 `T` 산술평균 (`statistics.mean`).

색: `Δ > 0` 상승색, `Δ < 0` 하락색.

---

### 3.3 CURRENT POSITION

관측 기간 전체에서의 상대 위치.

```
pos% = (T − min_all) / (max_all − min_all) × 100
```

- `min_all`, `max_all`: eligible 전체의 최저·최고 `T`  
- `max_all = min_all`이면 `pos% = 50`  
- 결과는 0~100으로 클램프  
- 오늘이 관측 최고면 `pos% = 100` (고정값이 아님)

막대 너비와 마커 `left`는 이 `pos%`를 CSS에 넣는다.

**현재 수준 `level`** (`pos%` 구간):

| pos% | 수준 |
|------|------|
| ≥80 | 매우 높음 |
| ≥60 | 높음 |
| ≥40 | 보통 |
| ≥20 | 낮음 |
| <20 | 매우 낮음 |

**변동성**

```
CV7 = pstdev(최근7일 T) / avg7 × 100
```

`pstdev` = 모표준편차 (`statistics.pstdev`). 표본 2일 미만이면 0.

| CV7 | 라벨 |
|-----|------|
| <8 | 낮음 |
| <15 | 보통 |
| <25 | 높음 |
| ≥25 | 매우 높음 |

게이지 너비:

```
vol_gauge% = min(100, CV7 / 30 × 100)
```

상수 `VOL_GAUGE_CAP = 30`. CV 21.6% → 게이지 72.0%.  
게이지는 시각 보조일 뿐, 본 지표는 CV7% 숫자다.

---

### 3.4 QUICK STATUS

최대 5행. 계산은 아래 규칙을 그대로 라벨화한 것이다.

#### 현재 활성도 수준
`level` (3.3).

#### 단기 추세
우선순위:

1. 가속도가 `상승 전환` / `하락 전환`이면 그 문구  
2. 연속 2일 이상이면 `N일 연속 상승|하락`  
3. 아니면 `Δ%` 구간  
   - ≥+5 상승 / ≤−5 하락  
   - +1~+5 소폭 상승 / −1~−5 소폭 하락  
   - 0 보합  

연속일수: 오늘←어제부터 같은 부호가 깨질 때까지의 일수. 0 변화는 연속을 끊는다.

가속도(참고, n≥3):

```
p1 = (T − T₋₁) / T₋₁ × 100
p0 = (T₋₁ − T₋₂) / T₋₂ × 100
```

| 조건 | 라벨 |
|------|------|
| p1>0, p0≤0 | 상승 전환 |
| p1<0, p0≥0 | 하락 전환 |
| 둘 다 + 이고 p1 > p0×1.2 | 상승 가속 |
| 둘 다 + 이고 p1 < p0×0.8 | 상승 둔화 |
| 둘 다 − 이고 \|p1\| > \|p0\|×1.2 | 하락 가속 |
| 둘 다 − 이고 \|p1\| < \|p0\|×0.8 | 하락 둔화 |
| 그 외 동부호 | 상승/하락 지속 |

#### 주간 추세
n≥14이면 주간 비교 `pct`(3.9):

- ≥+5 상승  
- ≤−5 하락  
- 그 사이 횡보  

n<14이면 최근 7일 첫값 대비 끝값 ±5%로 대체.

#### 국면 또는 기록
- 오늘 = 관측 최고 → `관측 최고`  
- 오늘 = 관측 최저 → `관측 최저`  
- 아니면 국면: `{고활성|보통|저활성} · {반등|조정|주간 상승|주간 하락|안정}`  

수준 태그: 매우 높음/높음=고활성, 매우 낮음/낮음=저활성, 나머지=보통.  
반등: 상승 전환이거나, 주간 하락인데 오늘 Δ>0.  
조정: 하락 전환이거나, 주간 상승인데 오늘 Δ<0.

#### 활성도 지수 (n≥30)
`score/100 · grade` + Beta 표기. 식은 3.6.

QUICK STATUS에 **변화 강도**(안정/소폭/큰 변화)는 넣지 않는다. TODAY·전일 %와 중복이기 때문이다.

---

### 3.5 TODAY'S BRIEFING

문장 생성 규칙. 숫자를 전부 재나열하지 않고 **결론 → 근거 → 추세 → 보조**만 쓴다.

1. 머리글 `brief_head`  
2. 판단문 `judgment`  
3. 근거 `num_sent` (`T`, `Δ`, `Δ%`)  
4. 추세문 `sent3` (주간 또는 고점/저점/수준)  
5. 보조 `aux` (지난주 동일 요일)

n≥30이면 `judgment`를 활성도 지수 문장으로 **덮어쓴다.**

```
오늘 케이브덕 활성도는 {grade} 수준입니다. 활성도 지수 {score}/100 (Beta).
```

머리글 충돌 방지: 단기 반등+주간 하락이면 `단기 반등 · 주간 하락`처럼 **두 시간축을 한 줄에 병기**한다.  
「단기 하락」과 「오늘 +13%」가 동시에 나오지 않게 한다.

원인 추정(이벤트, 신작, 점검 등)은 금지.

---

### 3.6 ACTIVITY INDEX (n≥30, 60일 미만 Beta)

30일 미만이면 섹션 자체를 출력하지 않는다.

구성 점수 (0~100으로 클램프 후 반올림):

```
score = 0.50×level_score
      + 0.25×trend_score
      + 0.15×momentum
      + 0.10×vol_score
```

| 항 | 정의 |
|----|------|
| level_score | 최근 30일 min~max 구간에서 오늘의 `rng` 위치 (0~100) |
| trend_score | 주간 추세 상승=70, 횡보=50, 하락=30 |
| momentum | Δ% ≥10 → 80, ≥0 → 60, >−10 → 40, 그 외 20 |
| vol_score | `max(0, 100 − CV7×3)` |

등급:

| score | grade |
|------:|-------|
| ≥81 | 매우 활발 |
| ≥61 | 활발 |
| ≥41 | 보통 |
| ≥21 | 저조 |
| <21 | 매우 저조 |

- 30≤n<60: **Beta** 표기  
- n≥60: Beta 제거 (정식)  
- 가중치와 구간은 아직 캘리브레이션 전이다. 감사 시 **상대 점수**로만 해석할 것.

같은 섹션에 30일 평균 대비를 덧붙인다.

```
avg30 = mean(최근 30일 T)
diff30 = T − avg30
pct30  = diff30 / avg30 × 100
```

---

### 3.7 ACTIVITY TREND

QuickChart 라인 차트. 값은 위와 동일한 `T` 시계열.

| 차트 | X | Y | 점선 |
|------|---|---|------|
| 7일 추이 | 최근 7일 `MM-DD (요일)` | 해당일 T | `avg7` |
| 월 추이 | 보고서 월(`YYYY-MM`)에 속한 일. 없으면 최근 30일 | T | 그 구간 평균 |

색: 7일 차트는 구간 첫값 대비 끝값이 같거나 크면 녹, 작으면 적.

모든 뷰포트에서 **1열**. 차트 2열 배치는 사용하지 않는다.

---

### 3.8 DAILY CHANGE

```
change_t% = (T_t − T_{t-1}) / T_{t-1} × 100
```

eligible 2일차부터 전 기간. 막대 색: ≥0 녹, <0 적.

---

### 3.9 COMPARISONS

카드 최대 5개 + 하단 메타.

| 카드 | 조건 | 식 |
|------|------|----|
| 전일 대비 | 전일 존재 | `Δ`, `Δ%`, 전일 T |
| 7일 평균 대비 | 항상 | `(T−avg7)/avg7×100`, avg7 |
| 요일 대비 (지난주 ○) | 과거 동일 요일 1회 이상 | 가장 최근 과거 동일 요일 `W`에 대해 `T−W`, `(T−W)/W×100` |
| 주간 비교 (최근 7일 vs 직전 7일) | n≥14 | `avg(최근7) − avg(직전7)` 및 그 비율. 표시: `직전평균 → 최근평균 · ▼N (−N%)` |
| 30일 평균 대비 | n≥30 | `T − avg30`, `(T−avg30)/avg30×100` |

주간 비교에서 최근/직전 7일을 한 번 더 나열하는 하위행은 두지 않는다.

메타:

- 7일 범위: `min(최근7) ~ max(최근7)`  
- 30일 평균: `avg30` (n≥30)  
- 관측 고점/저점: eligible 전체 argmax/argmin 의 T와 날짜  

---

### 3.10 WEEKDAY PATTERN · 최근 4주

조건: `n ≥ 21` 이고 요일 5개 이상에서 표본 ≥2.

방법:

1. eligible을 요일(월=0 … 일=6)로 분류  
2. 각 요일에서 **시간 상 마지막 최대 4개**만 사용 (주차를 나열하지 않음)  
3. 요일별 `n, avg, min, max, latest`  
4. 오늘 요일 행에 `· 오늘`  

인사이트 한 줄:

- 4주 평균이 가장 높은 요일 / 가장 낮은 요일  
- 오늘 값 vs 오늘 요일의 4주 평균  

막대 차트 Y값은 요일별 평균.

이 분석은 「요일 대비(지난주 하루)」와 다르다.  
요일 대비 = 직전 같은 요일 **하루**.  
4주 패턴 = 같은 요일 **최대 4일의 분포**.

---

### 3.11 RECENT RECORDS

eligible을 최신순으로 최대 60행.

| n | 표시 |
|---|------|
| <30 | 전체 |
| ≥30 | 최근 7행 + `전체 기록 보기`로 나머지 |

열: 날짜 `MM-DD (요일)` / 시각(`time_kst`) / 총합.  
모바일(≤560px)에서 시각 열 숨김.  
오늘 행 강조.

---

### 3.12 DATA STATUS / METHODOLOGY

기본 접힘. 매일 읽지 않는 감사·재현용 정보.

DATA STATUS:

- 관측 기간 `n일`, 시작일~종료일  
- 로우데이터 저장 시각  
- 기능 해금 표  

| 기능 | 해금 |
|------|------|
| 전일 비교 | n≥2 |
| 7일 추이 | n≥2 |
| 요일 비교 | 과거 동일 요일 존재 |
| 14일 주간 비교 | n≥14 |
| 30일 분석 · 활성도 지수 | n≥30 (60일 미만 Beta) |
| 90일 장기 | n≥90 (미도입) |

METHODOLOGY: Top20 합산 참고 지표라는 정의, 개별 캐릭터 분석 제외, mismatch 시 합산 우선, 브리핑 규칙.

FOOTER: Top20만 집계했다는 주의 + 저장 시각.

---

## 4. 표시 게이트 요약

| n | 켜지는 것 |
|---|-----------|
| 1 | TODAY 숫자만 |
| 2+ | 전일, 7일, 변동성, 단기 추세, 차트 |
| 동일 요일 과거 | 요일 대비 |
| 14+ | 주간 비교, 주간 추세 공식 전환 |
| 21+ (요일 표본 충족) | 4주 요일 패턴 |
| 30+ | 30일 평균, Activity Index Beta, Records 접기 |
| 60+ | Index 정식 (Beta 제거) |
| 90+ | 장기 분석 (아직 미구현) |

데이터가 부족하면 해당 블록을 **생략**한다. 0이나 가짜 평균을 넣지 않는다.

---

## 5. 재현 방법

```bash
cd scripts
python3 generate_report_html.py --date YYYY-MM-DD --out /tmp/out.html
```

입력: 로컬 `data/history.json` (원격과 병합된 캐시).  
동일 history + 동일 date면 HTML의 숫자·라벨은 결정적이다.  
차트 URL(QuickChart)은 같은 데이터로 재생성되므로 비트 단위 이미지 해시는 달라질 수 있으나 값은 같다.

감사 체크리스트:

1. `history.json`의 해당일 `total_top20` = TODAY 숫자  
2. `top20` 합산과 다르면 합산이 TODAY여야 함  
3. 전일 Δ = 오늘 − 어제  
4. 주간 비교 두 평균의 날짜 구간이 겹치지 않음 (최근7 / 직전7)  
5. pos%=100인 날은 그날 T가 eligible 최대인지  
6. Index는 n≥30에서만 존재하고 Beta 여부는 n<60  

---

## 6. 저장소 파일

| 경로 | 역할 |
|------|------|
| `data/latest.json` | 당일 원본 스냅샷 (덮어쓰기) |
| `data/history.json` | 일자별 확정 이력 |
| `reports/YYYY-MM-DD.html` | 해당일 HTML |
| `scripts/report_core.py` | 계산 |
| `scripts/report_template.py` | 마크업 |
| `scripts/generate_report_html.py` | 생성 엔트리 |
| `scripts/upload_github.py` | GitHub Contents API 업로드 |

Discord 임베드는 HTML의 축약본이며 이 명세의 계산을 다시 하지 않고 같은 `ctx`를 쓴다.
